server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: ${SERVER_CONTEXT_PATH:}

zuul:
  sensitiveHeaders:
  routes:
    management-service:
      path: /management/**
      serviceId: management-service