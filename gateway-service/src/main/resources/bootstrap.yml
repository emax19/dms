spring:
  application:
    name: gateway-service
  cloud:
    config:
      fail-fast: true
      uri: http://locahost:8100/
---
spring:
  profiles: docker
  cloud:
    config:
      fail-fast: true
      uri: http://config-service:8100/